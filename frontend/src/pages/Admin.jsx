import {useEffect,useState} from 'react';import {Users,BriefcaseBusiness,GraduationCap,TrendingUp,ShieldCheck,RefreshCw} from 'lucide-react';import api from '../services/api';
export default function Admin(){const [data,setData]=useState(null),[rows,setRows]=useState([]),[loading,setLoading]=useState(true);const load=()=>{setLoading(true);Promise.all([api.get('/api/admin/overview'),api.get('/api/admin/beneficiaries')]).then(([a,b])=>{setData(a.data.stats);setRows(b.data.beneficiaries)}).finally(()=>setLoading(false))};useEffect(load,[]);if(loading&&!data)return <div className='loading'>Loading admin workspace…</div>;return <section><div className='page-head'><div><div className='eyebrow'>ADMINISTRATION</div><h1>Programme control center</h1><p>Monitor beneficiary onboarding, livelihood pathways and programme-level activity from one place.</p></div><button className='btn' onClick={load}><RefreshCw size={16}/> Refresh</button></div><div className='stat-grid'>{[[Users,'Beneficiaries',data.beneficiaries],[ShieldCheck,'Active users',data.users],[GraduationCap,'Outcomes',data.outcomes],[BriefcaseBusiness,'Job roles',data.job_roles]].map(([I,k,v])=><div className='stat-card' key={k}><I/><span>{k}</span><b>{v}</b></div>)}</div><div className='panel'><div className='panel-head'><div><h2>Beneficiary accounts</h2><p>Recent users registered in VoxMinds.</p></div><span className='admin-badge'>ADMIN ONLY</span></div><div className='table-wrap'><table><thead><tr><th>Name</th><th>Location</th><th>Education</th><th>Preference</th><th>Joined</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><b>{r.name}</b></td><td>{r.district||'—'}, {r.state||''}</td><td>{r.education||'—'}</td><td>{r.employment_preference||'—'}</td><td>{r.created_at?new Date(r.created_at).toLocaleDateString():'—'}</td></tr>)}</tbody></table>{!rows.length&&<div className='empty'>No beneficiary accounts yet.</div>}</div></div></section>}
